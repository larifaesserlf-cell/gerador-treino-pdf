name: Keep Streamlit app awake

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -sSf https://2rve8kptjdtf3tcvekxhc7.streamlit.app > /dev/null
